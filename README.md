Creates a snapshot of the desired spotify playlists with track information and exports it to a timestamped csv file.
By comparing two exports tracking the changes of playlists content is possible, thus can be learned if any tracks were removed by spotify.
